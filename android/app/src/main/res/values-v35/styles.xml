<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      UCrop theme for Android 15+ — also opt out of edge-to-edge enforcement.
      NOTE: with targetSdk 36, `windowOptOutEdgeToEdgeEnforcement` is IGNORED on Android 16
      devices (it still works on Android 15). What keeps the ✓ / X tappable there is
      `windowIsFloating` + `fitsSystemWindows` below: floating windows are exempt from
      edge-to-edge enforcement and insets are still applied to the content view.
    -->
    <style name="UCropTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">#424242</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
