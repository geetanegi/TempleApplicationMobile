<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <!--
      enableOnBackInvokedCallback=false: with targetSdk 36 predictive back is on by default,
      and Android 16 then stops calling Activity.onBackPressed() / dispatching KEYCODE_BACK.
      React Native 0.77.3 has no OnBackInvokedCallback support (ReactActivity.onBackPressed is
      the only path), so BackHandler and react-navigation's hardware-back handling would go
      dead. Opting out keeps the legacy back dispatch. Remove this once RN supports predictive
      back - Google will drop the opt-out in a future release.
    -->
    <application
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="false"
        android:theme="@style/AppTheme">

        <!--
          targetSdk 36 (Android 16) ignores android:screenOrientation and
          setRequestedOrientation() on screens whose smallest width is >= 600dp (tablets,
          unfolded foldables, desktop windowing). The app is portrait-only by design -
          src/App.js calls Orientation.lockToPortrait() and PostCard toggles it for video -
          so opt back into the restriction. Temporary: Google will stop honouring this
          property once we target API 37, at which point the layouts need to handle
          landscape/resizing on large screens for real.
        -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
            android:value="true" />
        <!-- Google Maps API Key - replace with your key from Google Cloud Console -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDK6UoNV-QPxx0KJHH1NvwREj8n2tKdmZU"/>

        <!-- FCM: use same channel as Notifee / backend AndroidConfig (background system tray) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="social"
            tools:replace="android:value" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>

        </activity>

        <!-- Override UCrop (image crop) activity theme to fix status bar overlap -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:theme="@style/UCropTheme"
            android:exported="false"
            tools:replace="android:theme" />
        <!-- Foreground Service Declaration -->
      <service
    android:name="com.asterinet.react.bgactions.RNBackgroundActionsTask"
    android:permission="android.permission.BIND_JOB_SERVICE"
    android:exported="true"/>

    </application>
</manifest>
